apiVersion: v1
kind: Secret
metadata:
  name: observability-repo
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: repository
stringData:
  # Public repo: only URL is required
  url: https://github.com/visheshofficial/observability
  project: default
  name: observability

