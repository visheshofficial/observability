apiVersion: v1
kind: Service
metadata:
  name: httpbin
  namespace: istioinaction-ch01
  labels:
    app: httpbin
spec:
  selector:
    app: httpbin
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
